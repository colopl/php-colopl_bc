#!/bin/sh
set -e

case "$1" in
    remove|deconfigure)
        if command -v phpdismod >/dev/null 2>&1; then
            phpdismod -v 8.4 -s ALL colopl_bc || true
        fi
        ;;
esac

#DEBHELPER#

exit 0